parameters:
    package_param_overridable_on_app_level: 'default_value'

    just_to_use_it: '%env(APP_SECRET)%'

services:
    _defaults:
        public: true

    Symplify\FlexLoader\Tests\Flex\FlexLoader\Source\ExtraService: ~
