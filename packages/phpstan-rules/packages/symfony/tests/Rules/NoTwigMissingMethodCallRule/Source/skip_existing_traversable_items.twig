some {{ some_iterator_type.some_child }}
some {{ some_iterator_type.parent }}

some {{ some_iterator_type.existingMethod }}

some {{ some_iterator_type.existingMethod|whatever_filter }}

some {{ whatever_function(some_iterator_type.existingMethod) }}
