parameters:
    enable_default_release_workers: true

services:
    _defaults:
        public: true
        autowire: true

    Symplify\MonorepoBuilder\Release\:
        resource: '../src'
        exclude: '../src/Exception'
