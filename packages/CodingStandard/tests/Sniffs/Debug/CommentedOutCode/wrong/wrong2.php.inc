<?php

//try {
//    $parser = (new ParserFactory)->create(ParserFactory::PREFER_PHP7);
//    $tokens = $parser->parse($content);
//    $isCode = true;
//
//    if (count($tokens) === 1 && $tokens[0] instanceof ConstFetch) {
//        $isCode = false;
//    }
//
//} catch (Error $error) {
//    $isCode = false;
//}
