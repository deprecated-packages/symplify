---
layout: "_layouts/default.twig"
title: "Welcome to my super-cool Statie page!"
---

{% block content %}
    {# title is taken from "title:" above #}
    <p>{{ title }}</p>

    <h2>I'm Workaholic and These are my Babies</h2>

    <ul>
        {% for projectUrl, projectName in projects %}
            <li>
                <a href="{{ projectUrl }}">{{ projectName }}</a>
            </li>
        {% endfor %}
    </ul>

    <hr>

    <h2>I also Think a lot... and Write</h2>

    <ul>
        {% for post in posts %}
            <li>
                {# post.relativeUrl is internal variable with relative url to the post #}
                <a href="/{{ post.relativeUrl }}/">
                    {{ post.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
